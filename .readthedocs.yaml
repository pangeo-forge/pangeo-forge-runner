version: 2

sphinx:
  configuration: docs/conf.py

python:
  version: "3.9"
  install:
    # Install package too, so autodoc works
    - method: pip
      path: .
    - requirements: docs/requirements.txt

formats:
  - htmlzip
  - epub
